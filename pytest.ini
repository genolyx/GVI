[pytest]
testpaths = engine/tests
addopts = -q
# Repo root makes `engine.service.*` importable; `engine/` makes the vendored
# `vc_engine` package importable as a top-level module, which `app_v11.py` requires.
pythonpath = . engine

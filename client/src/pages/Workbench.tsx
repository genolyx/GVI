import { AIChatBox, type Message } from "@/components/AIChatBox";
import { ClinicalStatus } from "@/components/ClinicalStatus";
import { PageHeader } from "@/components/PageHeader";
import { StatePanel } from "@/components/StatePanel";
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { ScrollArea } from "@/components/ui/scroll-area";
import { Skeleton } from "@/components/ui/skeleton";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Textarea } from "@/components/ui/textarea";
import { useOrganization } from "@/contexts/OrganizationContext";
import { trpc } from "@/lib/trpc";
import { ACMG_CRITERIA, GERMLINE_CLASSIFICATIONS, ONCOGENICITY_CLASSIFICATIONS, SOMATIC_TIERS } from "@shared/clinical-standards";
import { ArrowLeft, BookOpen, Bot, CheckCircle2, ChevronRight, ExternalLink, Filter, FlaskConical, Loader2, RefreshCw, Save, Search, ShieldAlert, Sparkles } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { toast } from "sonner";
import { useLocation, useParams } from "wouter";

type Impact = "HIGH" | "MODERATE" | "LOW" | "MODIFIER" | "UNKNOWN";
type CriterionState = "met" | "not_met" | "not_applicable";

export default function WorkbenchPage() {
  const params = useParams<{ caseId: string }>();
  const caseId = Number(params.caseId);
  const { activeOrganizationId, hasPermission } = useOrganization();
  const [, navigate] = useLocation();
  const [search, setSearch] = useState("");
  const [impact, setImpact] = useState<Impact | "all">("all");
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [modelId, setModelId] = useState("gpt-5-mini");
  const [conversationId, setConversationId] = useState<number | undefined>();
  const [localMessages, setLocalMessages] = useState<Message[]>([]);
  const [classification, setClassification] = useState<string>("");
  const [somaticTier, setSomaticTier] = useState<string>("");
  const [oncogenicity, setOncogenicity] = useState<string>("");
  const [rationale, setRationale] = useState("");
  const [diseaseContext, setDiseaseContext] = useState("");
  const [activeCriterion, setActiveCriterion] = useState<(typeof ACMG_CRITERIA)[number]>("PVS1");
  const [criterionState, setCriterionState] = useState<CriterionState>("not_met");
  const [criterionNote, setCriterionNote] = useState("");
  const utils = trpc.useUtils();

  const canReadVariants = hasPermission("variant:read");
  const caseQuery = trpc.cases.get.useQuery({ organizationId: activeOrganizationId || 0, caseId }, { enabled: Boolean(activeOrganizationId && caseId && canReadVariants) });
  const list = trpc.variants.list.useQuery({ organizationId: activeOrganizationId || 0, caseId, search: search || undefined, impact: impact === "all" ? undefined : impact, sortBy: "impact", sortDirection: "asc", limit: 500 }, { enabled: Boolean(activeOrganizationId && caseId && canReadVariants) });
  useEffect(() => { if (!selectedId && list.data?.[0]) setSelectedId(list.data[0].id); }, [list.data, selectedId]);
  const detail = trpc.variants.detail.useQuery({ organizationId: activeOrganizationId || 0, variantId: selectedId || 0 }, { enabled: Boolean(activeOrganizationId && selectedId && canReadVariants) });
  const models = trpc.copilot.models.useQuery(undefined, { staleTime: 60_000, enabled: canReadVariants });
  const messageQuery = trpc.copilot.messages.useQuery({ organizationId: activeOrganizationId || 0, conversationId: conversationId || 0 }, { enabled: Boolean(activeOrganizationId && conversationId && canReadVariants) });
  useEffect(() => { if (messageQuery.data) setLocalMessages(messageQuery.data.messages.map(message => ({ role: message.role, content: message.content }))); }, [messageQuery.data]);
  useEffect(() => {
    const current = detail.data?.interpretations[0];
    setClassification(current?.germlineClassification || "");
    setSomaticTier(current?.somaticTier || "");
    setOncogenicity(current?.oncogenicity || "");
    setRationale(current?.rationale || "");
    setDiseaseContext(current?.diseaseContext || "");
    setConversationId(detail.data?.conversations[0]?.id);
    setLocalMessages([]);
  }, [selectedId, detail.data?.interpretations, detail.data?.conversations]);

  const refresh = trpc.variants.refreshEvidence.useMutation({ onSuccess: async result => { await detail.refetch(); toast.success(`${result.count}개 근거를 Evidence Ledger에 추가했습니다.`); }, onError: error => toast.error(error.message) });
  const saveInterpretation = trpc.variants.saveInterpretation.useMutation({ onSuccess: async () => { await Promise.all([detail.refetch(), list.refetch()]); toast.success("판정 초안을 저장했습니다."); }, onError: error => toast.error(error.message) });
  const saveCriterion = trpc.variants.saveCriterion.useMutation({ onSuccess: async () => { await detail.refetch(); toast.success(`${activeCriterion} 평가를 저장했습니다.`); }, onError: error => toast.error(error.message) });
  const approve = trpc.variants.approveInterpretation.useMutation({ onSuccess: async () => { await Promise.all([detail.refetch(), list.refetch()]); toast.success("전문의 승인이 기록되었습니다."); }, onError: error => toast.error(error.message) });
  const ask = trpc.copilot.ask.useMutation({
    onSuccess: result => { setConversationId(result.conversationId); setLocalMessages(current => [...current, { role: "assistant", content: result.content }]); toast.success(`인용 ${result.citationIds.length}개를 검증했습니다.`); },
    onError: error => { toast.error(error.message); setLocalMessages(current => current.slice(0, -1)); },
  });
  const currentInterpretation = detail.data?.interpretations[0];
  const criteriaMap = useMemo(() => new Map(detail.data?.criteria.map(item => [item.code, item.state]) || []), [detail.data?.criteria]);
  const handleSave = (submitForReview: boolean) => {
    if (!selectedId || !caseQuery.data) return;
    saveInterpretation.mutate({ organizationId: activeOrganizationId!, variantId: selectedId, germlineClassification: caseQuery.data.purpose === "germline" ? classification as (typeof GERMLINE_CLASSIFICATIONS)[number] : undefined, somaticTier: caseQuery.data.purpose === "somatic" ? somaticTier as (typeof SOMATIC_TIERS)[number] : undefined, oncogenicity: caseQuery.data.purpose === "somatic" ? oncogenicity as (typeof ONCOGENICITY_CLASSIFICATIONS)[number] : undefined, diseaseContext: diseaseContext || undefined, rationale, submitForReview });
  };
  const sendMessage = (question: string) => { if (!selectedId) return; setLocalMessages(current => [...current, { role: "user", content: question }]); ask.mutate({ organizationId: activeOrganizationId!, variantId: selectedId, conversationId, modelId: conversationId ? undefined : modelId, question }); };
  const failedMutation = refresh.error ? { title: "공개 근거 갱신에 실패했습니다", message: refresh.error.message, retry: () => refresh.variables && refresh.mutate(refresh.variables) }
    : saveInterpretation.error ? { title: "판정 저장에 실패했습니다", message: saveInterpretation.error.message, retry: () => saveInterpretation.variables && saveInterpretation.mutate(saveInterpretation.variables) }
    : saveCriterion.error ? { title: "ACMG 기준 저장에 실패했습니다", message: saveCriterion.error.message, retry: () => saveCriterion.variables && saveCriterion.mutate(saveCriterion.variables) }
    : approve.error ? { title: "전문의 승인에 실패했습니다", message: approve.error.message, retry: () => approve.variables && approve.mutate(approve.variables) }
    : ask.error ? { title: "Copilot 응답 생성에 실패했습니다", message: ask.error.message, retry: () => { if (!ask.variables) return; setLocalMessages(current => [...current, { role: "user", content: ask.variables!.question }]); ask.mutate(ask.variables); } }
    : null;

  if (!canReadVariants) return <div className="space-y-7"><PageHeader eyebrow="Variant interpretation" title="변이 워크벤치" description="조직 범위의 변이와 임상 근거를 검토합니다." /><StatePanel type="forbidden" title="변이 조회 권한이 없습니다" description="조직 관리자에게 variant:read 액션이 포함된 역할을 요청하십시오." action={<Button variant="outline" onClick={() => navigate("/")}>대시보드</Button>} /></div>;
  if (caseQuery.isLoading) return <div className="space-y-5"><Skeleton className="h-20" /><Skeleton className="h-[650px]" /></div>;
  if (caseQuery.isError) return <div className="space-y-7"><PageHeader eyebrow="Variant interpretation" title="워크벤치 조회 오류" description="요청한 케이스와 조직 접근 경계를 확인할 수 없습니다." /><StatePanel type="error" title="워크벤치를 불러오지 못했습니다" description={caseQuery.error.message} onRetry={() => { void caseQuery.refetch(); }} action={<Button variant="outline" onClick={() => navigate(`/cases/${caseId}`)}><ArrowLeft className="mr-2 size-4" />케이스</Button>} /></div>;
  if (!caseQuery.data) return <div className="space-y-7"><PageHeader eyebrow="Variant interpretation" title="워크벤치 없음" description="요청한 케이스 식별자를 확인하십시오." /><StatePanel type="empty" title="케이스를 찾을 수 없습니다" description="현재 조직에서 접근할 수 없는 케이스는 워크벤치에 노출되지 않습니다." action={<Button variant="outline" onClick={() => navigate("/cases")}><ArrowLeft className="mr-2 size-4" />케이스 목록</Button>} /></div>;
  const clinicalCase = caseQuery.data;
  return (
    <div className="space-y-5">
      <PageHeader eyebrow={`${clinicalCase.purpose} interpretation`} title={`${clinicalCase.caseNumber} · 변이 워크벤치`} description={`${clinicalCase.patientAlias} · ${clinicalCase.referenceBuild} · ${clinicalCase.variantCount.toLocaleString()} variants`} badge={clinicalCase.status} actions={<Button variant="outline" onClick={() => navigate(`/cases/${caseId}`)}><ArrowLeft className="mr-2 size-4" />케이스</Button>} />
      <Alert className="border-amber-200 bg-amber-50/65 text-amber-950"><ShieldAlert className="size-4 text-amber-700" /><AlertTitle>전문가 판정 영역</AlertTitle><AlertDescription className="text-amber-800/80">AI 초안은 판정과 보고 서명을 대체하지 않습니다. 적용 기준과 근거를 검토한 전문가가 최종 상태를 승인해야 합니다.</AlertDescription></Alert>
      {failedMutation ? <StatePanel compact type="error" title={failedMutation.title} description={failedMutation.message} onRetry={failedMutation.retry} /> : null}
      <div className="grid min-h-[720px] overflow-hidden rounded-2xl border border-border/80 bg-card shadow-[0_18px_50px_-36px_rgba(15,23,42,.28)] xl:grid-cols-[minmax(540px,1.15fr)_minmax(470px,.85fr)]">
        <section className="min-w-0 border-b border-border/70 xl:border-b-0 xl:border-r">
          <div className="flex flex-col gap-3 border-b border-border/70 bg-muted/20 p-4 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><Input aria-label="유전자, HGVS 또는 정규화 ID 검색" value={search} onChange={event => setSearch(event.target.value)} placeholder="Gene, HGVS, normalized ID" className="pl-9" /></div><select aria-label="변이 영향도 필터" value={impact} onChange={event => setImpact(event.target.value as Impact | "all")} className="h-10 rounded-lg border border-input bg-background px-3 text-xs"><option value="all">All impact</option>{["HIGH", "MODERATE", "LOW", "MODIFIER", "UNKNOWN"].map(value => <option key={value}>{value}</option>)}</select></div>
          <ScrollArea className="h-[655px]"><table className="w-full min-w-[690px] text-left"><thead className="sticky top-0 z-10 bg-card"><tr className="border-b border-border/70 text-[9px] uppercase tracking-[.14em] text-muted-foreground"><th className="px-4 py-3">Variant</th><th className="px-3 py-3">Impact</th><th className="px-3 py-3">Frequency</th><th className="px-3 py-3">Classification</th><th className="w-8" /></tr></thead><tbody className="divide-y divide-border/55">{list.isError ? <tr><td colSpan={5} className="p-4"><StatePanel compact type="error" title="변이 목록을 불러오지 못했습니다" description={list.error.message} onRetry={() => { void list.refetch(); }} /></td></tr> : list.isLoading ? Array.from({ length: 10 }).map((_, index) => <tr key={index}><td colSpan={5} className="p-3"><Skeleton className="h-10" /></td></tr>) : list.data?.map(variant => <tr key={variant.id} tabIndex={0} role="button" aria-pressed={selectedId === variant.id} aria-label={`${variant.gene || "Intergenic"} ${variant.hgvsC || variant.normalizedId} 변이 선택`} onKeyDown={event => { if (event.key === "Enter" || event.key === " ") { event.preventDefault(); setSelectedId(variant.id); } }} onClick={() => setSelectedId(variant.id)} className={`cursor-pointer transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-primary ${selectedId === variant.id ? "bg-primary/[0.055]" : "hover:bg-muted/40"}`}><td className="px-4 py-3"><div className="flex items-center gap-3"><div className={`h-8 w-1 rounded-full ${variant.reviewStatus === "flagged" ? "bg-amber-500" : variant.reviewStatus === "reviewed" ? "bg-emerald-500" : "bg-slate-200"}`} /><div><p className="font-mono text-[11px] font-semibold">{variant.gene || "Intergenic"} <span className="font-normal text-muted-foreground">{variant.hgvsC || variant.normalizedId}</span></p><p className="mt-1 max-w-[285px] truncate text-[10px] text-muted-foreground">{variant.hgvsP || variant.consequence || "—"}</p></div></div></td><td className="px-3 py-3"><Badge variant="outline" className={`text-[9px] ${variant.impact === "HIGH" ? "border-rose-200 bg-rose-50 text-rose-700" : variant.impact === "MODERATE" ? "border-amber-200 bg-amber-50 text-amber-700" : ""}`}>{variant.impact}</Badge></td><td className="px-3 py-3 font-mono text-[10px]">{variant.populationAf ? Number(variant.populationAf).toExponential(2) : "—"}<p className="mt-1 text-[9px] text-muted-foreground">VAF {variant.vaf ? `${(Number(variant.vaf) * 100).toFixed(1)}%` : "—"}</p></td><td className="px-3 py-3"><p className="text-[10px] font-medium">{variant.germlineClassification || variant.somaticTier || "Unclassified"}</p><p className="mt-1 text-[9px] text-muted-foreground">{variant.oncogenicity || variant.interpretationStatus || "Draft required"}</p></td><td><ChevronRight className="size-4 text-muted-foreground/50" /></td></tr>)}</tbody></table></ScrollArea>
        </section>
        <section className="min-w-0 bg-muted/[0.12]">
          {detail.isError ? <div className="p-5"><StatePanel type="error" title="변이 상세를 불러오지 못했습니다" description={detail.error.message} onRetry={() => { void detail.refetch(); }} /></div> : !selectedId || detail.isLoading ? <div className="space-y-4 p-5"><Skeleton className="h-20" /><Skeleton className="h-[560px]" /></div> : detail.data ? <div><div className="border-b border-border/70 bg-card p-5"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><h2 className="font-display text-lg font-semibold">{detail.data.variant.gene || "Intergenic"}</h2><Badge variant="outline">{detail.data.variant.variantType}</Badge></div><p className="mt-2 font-mono text-xs text-muted-foreground">{detail.data.variant.hgvsC || detail.data.variant.normalizedId}</p><p className="mt-1 font-mono text-[11px] text-muted-foreground">{detail.data.variant.hgvsP}</p></div><ClinicalStatus status={detail.data.variant.reviewStatus} /></div></div>
            <Tabs defaultValue="evidence" className="p-4"><TabsList className="grid w-full grid-cols-3"><TabsTrigger value="evidence"><BookOpen className="mr-2 size-3.5" />근거</TabsTrigger><TabsTrigger value="classification"><FlaskConical className="mr-2 size-3.5" />판정</TabsTrigger><TabsTrigger value="copilot"><Sparkles className="mr-2 size-3.5" />AI Copilot</TabsTrigger></TabsList>
              <TabsContent value="evidence" className="mt-4"><div className="mb-3 flex items-center justify-between"><div><p className="text-sm font-semibold">Evidence Ledger</p><p className="text-[10px] text-muted-foreground">출처 · 접근시각 · 임상축 보존</p></div>{hasPermission("interpretation:edit") ? <Button size="sm" variant="outline" onClick={() => refresh.mutate({ organizationId: activeOrganizationId!, variantId: selectedId })} disabled={refresh.isPending}>{refresh.isPending ? <Loader2 className="mr-2 size-3.5 animate-spin" /> : <RefreshCw className="mr-2 size-3.5" />}공개 근거 갱신</Button> : null}</div><ScrollArea className="h-[490px] pr-3"><div className="space-y-3">{detail.data.evidence.length ? detail.data.evidence.map(item => <Card key={item.id} className="shadow-none"><CardContent className="p-4"><div className="flex items-start justify-between gap-3"><div className="flex flex-wrap gap-1.5"><Badge variant="secondary">E{item.id}</Badge><Badge variant="outline">{item.source}</Badge><Badge variant="outline">{item.clinicalDomain}</Badge></div>{item.url ? <a href={item.url} target="_blank" rel="noreferrer" className="text-muted-foreground hover:text-primary"><ExternalLink className="size-3.5" /></a> : null}</div><p className="mt-3 text-xs font-semibold leading-5">{item.title}</p><p className="mt-2 text-[11px] leading-5 text-muted-foreground">{item.excerpt}</p><div className="mt-3 flex items-center justify-between border-t border-border/60 pt-2 text-[9px] text-muted-foreground"><span>{item.direction} · {item.evidenceLevel || "level not assigned"}</span><span>{new Date(item.accessedAt).toLocaleDateString()}</span></div></CardContent></Card>) : <div className="rounded-xl border border-dashed py-14 text-center"><BookOpen className="mx-auto size-7 text-muted-foreground/35" /><p className="mt-3 text-xs font-medium">저장된 근거가 없습니다</p><p className="mt-1 text-[10px] text-muted-foreground">공개 근거 갱신 후 전문가가 출처를 검토하십시오.</p></div>}<div className="mt-5 border-t border-border/70 pt-4"><p className="mb-3 text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">Variant audit history</p>{detail.data.audit.length ? detail.data.audit.map(event => <div key={event.id} className="mb-2 flex items-start justify-between gap-3 rounded-lg bg-muted/50 px-3 py-2"><div><p className="text-[10px] font-medium">{event.action}</p><p className="mt-1 font-mono text-[9px] text-muted-foreground">request {event.requestId.slice(0, 12)}</p></div><span className="whitespace-nowrap text-[9px] text-muted-foreground">{new Date(event.createdAt).toLocaleString()}</span></div>) : <p className="text-[10px] text-muted-foreground">아직 기록된 변이 변경 이벤트가 없습니다.</p>}</div></div></ScrollArea></TabsContent>
              {hasPermission("interpretation:edit") ? <TabsContent value="classification" className="mt-4"><ScrollArea className="h-[540px] pr-3"><div className="space-y-5">{clinicalCase.purpose === "germline" ? <><div className="rounded-xl border border-teal-200 bg-teal-50/60 p-4"><p className="text-[10px] font-semibold uppercase tracking-wider text-teal-700">Rule-based suggestion</p><p className="mt-2 text-sm font-semibold text-teal-950">{detail.data.acmgSuggestion?.classification}</p><p className="mt-1 text-[11px] leading-5 text-teal-800">{detail.data.acmgSuggestion?.rationale}</p></div><div className="space-y-2"><Label>최종 Germline 분류</Label><select value={classification} onChange={event => setClassification(event.target.value)} className="h-10 w-full rounded-lg border border-input bg-background px-3 text-sm"><option value="">분류 선택</option>{GERMLINE_CLASSIFICATIONS.map(value => <option key={value}>{value}</option>)}</select></div><div><div className="mb-2 flex items-center justify-between"><Label>ACMG 2015 criteria</Label><span className="text-[9px] text-muted-foreground">코드를 선택해 평가 기록</span></div><div className="grid grid-cols-6 gap-1.5">{ACMG_CRITERIA.map(code => <button key={code} onClick={() => { setActiveCriterion(code); const current = detail.data.criteria.find(item => item.code === code); setCriterionState((current?.state as CriterionState) || "not_met"); setCriterionNote(current?.note || ""); }} className={`rounded-md border px-1 py-2 font-mono text-[9px] font-semibold ${activeCriterion === code ? "border-primary bg-primary text-primary-foreground" : criteriaMap.get(code) === "met" ? "border-emerald-200 bg-emerald-50 text-emerald-700" : "border-border bg-card text-muted-foreground"}`}>{code}</button>)}</div><div className="mt-3 rounded-xl border border-border bg-card p-3"><div className="flex items-center justify-between"><p className="font-mono text-xs font-semibold">{activeCriterion}</p><select value={criterionState} onChange={event => setCriterionState(event.target.value as CriterionState)} className="h-8 rounded-md border border-input bg-background px-2 text-[10px]"><option value="met">Met</option><option value="not_met">Not met</option><option value="not_applicable">N/A</option></select></div><Textarea value={criterionNote} onChange={event => setCriterionNote(event.target.value)} className="mt-2 min-h-20 text-xs" placeholder="적용 근거 또는 제외 사유를 기록하십시오." /><Button size="sm" variant="outline" className="mt-2 w-full" disabled={!currentInterpretation || saveCriterion.isPending || (criterionState === "met" && criterionNote.trim().length < 2)} onClick={() => currentInterpretation && saveCriterion.mutate({ organizationId: activeOrganizationId!, interpretationId: currentInterpretation.id, code: activeCriterion, state: criterionState, evidenceIds: [], note: criterionNote || undefined })}>{currentInterpretation ? "Criteria 저장" : "먼저 판정 초안을 저장하십시오"}</Button></div></div></> : <><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label>AMP/ASCO/CAP Tier</Label><select value={somaticTier} onChange={event => setSomaticTier(event.target.value)} className="h-10 w-full rounded-lg border border-input bg-background px-3 text-sm"><option value="">Tier 선택</option>{SOMATIC_TIERS.map(value => <option key={value}>{value}</option>)}</select></div><div className="space-y-2"><Label>Oncogenicity</Label><select value={oncogenicity} onChange={event => setOncogenicity(event.target.value)} className="h-10 w-full rounded-lg border border-input bg-background px-3 text-sm"><option value="">분류 선택</option>{ONCOGENICITY_CLASSIFICATIONS.map(value => <option key={value}>{value}</option>)}</select></div></div><div className="space-y-2"><Label>질환 맥락</Label><Input value={diseaseContext} onChange={event => setDiseaseContext(event.target.value)} placeholder="암종, stage, line of therapy" /></div><div className="grid grid-cols-3 gap-2">{["Predictive", "Diagnostic", "Prognostic"].map(domain => <div key={domain} className="rounded-xl border border-border bg-card p-3 text-center"><p className="text-[10px] font-semibold">{domain}</p><p className="mt-1 text-[9px] text-muted-foreground">근거 패널과 독립 연결</p></div>)}</div></>}
                <div className="space-y-2"><Label>전문가 판정 근거 메모</Label><Textarea value={rationale} onChange={event => setRationale(event.target.value)} className="min-h-32" placeholder="근거의 적용, 상충 정보, 질환 맥락과 제한점을 기술하십시오." /></div><div className="grid gap-2 sm:grid-cols-2"><Button variant="outline" disabled={saveInterpretation.isPending || rationale.trim().length < 20} onClick={() => handleSave(false)}><Save className="mr-2 size-4" />Draft 저장</Button><Button disabled={saveInterpretation.isPending || rationale.trim().length < 20} onClick={() => handleSave(true)}><CheckCircle2 className="mr-2 size-4" />In Review 제출</Button></div>{currentInterpretation?.status === "in_review" && hasPermission("interpretation:approve") ? <Button className="w-full" variant="secondary" disabled={approve.isPending} onClick={() => approve.mutate({ organizationId: activeOrganizationId!, interpretationId: currentInterpretation.id })}>전문의 승인</Button> : null}</div></ScrollArea></TabsContent> : null}
              <TabsContent value="copilot" className="mt-4"><div className="mb-3 flex items-center justify-between gap-3"><div><p className="text-sm font-semibold">Evidence Copilot</p><p className="text-[10px] text-muted-foreground">Ledger-only · cited draft · no sign-out</p></div><select aria-label="Copilot 모델" value={modelId} onChange={event => setModelId(event.target.value)} disabled={Boolean(conversationId) || models.isError} className="h-8 max-w-44 rounded-md border border-input bg-background px-2 text-[10px]">{models.data?.map(model => <option key={model.id} value={model.id}>{model.id}</option>) || <option value="gpt-5-mini">gpt-5-mini</option>}</select></div>{models.isError ? <StatePanel compact type="error" title="AI 모델 카탈로그를 불러오지 못했습니다" description={models.error.message} onRetry={() => { void models.refetch(); }} /> : messageQuery.isError ? <StatePanel compact type="error" title="Copilot 대화 기록을 불러오지 못했습니다" description={messageQuery.error.message} onRetry={() => { void messageQuery.refetch(); }} /> : <AIChatBox messages={localMessages} onSendMessage={sendMessage} isLoading={ask.isPending || messageQuery.isLoading} height={500} placeholder="현재 변이와 저장된 근거에 관해 질문하십시오…" emptyStateMessage="검토된 Evidence Ledger만 사용해 인용 가능한 초안을 만듭니다." suggestedPrompts={["이 변이의 병원성 근거와 상충 근거를 분리해 요약해줘.", "현재 근거에서 추가로 검토해야 할 공백은 무엇이야?", "보고서용 해석 초안을 근거 ID와 함께 작성해줘."]} />}</TabsContent>
            </Tabs></div> : null}
        </section>
      </div>
    </div>
  );
}
